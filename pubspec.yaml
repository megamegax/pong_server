name: pong_server
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  dart_frog: ^1.1.0
  dart_mappable: ^4.2.2

dev_dependencies:
  build_runner: ^2.4.12
  dart_mappable_builder: ^4.2.3
  mocktail: ^1.0.3
  test: ^1.25.5
  very_good_analysis: ^5.1.0
